
import { Injectable } from '@angular/core';


@Injectable({
  providedIn: 'root'
})
export class ProfissionaisService {


  /*
  constructor(private http:HttpClient) {}

  // Método para editar um profissional
  updateProfissional(id: string, profissional: any): Observable<any> {
    return this.http.put(`${this.apiUrl}/${id}`, profissional);
  }

  // Método para excluir um profissional
  deleteProfissional(id: string): Observable<any> {
    return this.http.delete(`${this.apiUrl}/${id}`);
  }
  */
}
